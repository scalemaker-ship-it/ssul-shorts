#!/usr/bin/env python3
"""화면 규격 — 레퍼런스 프레임(414x738) 실측값을 1080x1920 로 옮긴 것.

측정 원본: N잡연구소 B채널 쇼츠 템플릿
밴드 경계는 전부 세로 비율(%)로 보관해서 해상도를 바꿔도 그대로 유지된다.
"""

W, H = 1080, 1920                       # 9:16

# ── 세로 밴드 (레퍼런스 실측 %) ────────────────────────────────
TITLE_TOP    = 0.127                    # 타이틀 1행 시작
TITLE_MID    = 0.190                    # 1행/2행 경계
TITLE_BOT    = 0.269                    # 타이틀 끝
CONTENT_TOP  = 0.275                    # 콘텐츠(이미지) 시작 — 서브타이틀 자리까지 확장
CONTENT_BOT  = 0.710                    # 콘텐츠 끝
SUB_CENTER   = 0.745                    # 자막 세로 중심 — 이미지 바로 밑 검정 영역

# 유튜브 쇼츠 UI 가 덮는 구간. 채널명·설명·진행바가 여기 올라온다.
# 이 아래로는 어떤 요소도 두지 않는다.
DEADZONE_TOP = 0.855
# 이미지 아래 검정 영역(0.710~0.855) 중 자막을 뺀 남은 공간의 중앙에 배치.
# 원래 0.912/0.952 였으나 쇼츠 UI 에 가려져 위로 올렸다.
PROFILE_Y    = 0.800                    # 프로필 원 중심
PROFILE_R    = 0.022                    # 프로필 반지름
CHANNEL_Y    = 0.828                    # 채널명 baseline

# 하단 프로필 원 + 채널명. 유튜브가 바로 아래에 같은 정보를 이미 띄우고,
# 화면만 복잡해져서 껐다. 채널 표기는 이미지 안 워터마크가 맡는다.
SHOW_EMBLEM  = False

# ── 색 (레퍼런스 실측) ────────────────────────────────────────
BG        = (0, 0, 0)                   # #000000
NEON      = (251, 255, 86)              # #FBFF56  형광 노랑 (두 레퍼런스 공통)
WHITE     = (255, 255, 255)

# 타이틀 배색 — 자나스 방식: 1행 흰색(관찰) / 2행 노랑(키워드)
# N잡연구소 방식으로 되돌리려면 아래 두 줄을 서로 바꾼다
TITLE_1   = WHITE
TITLE_2   = NEON
BAR_BG    = (255, 255, 255)             # #FFFFFF
BAR_TEXT  = (17, 17, 17)
BAR_CNT   = (47, 105, 236)              # 흰 바의 댓글 수 (000) — 커뮤니티 파란색
SUB_TEXT  = (255, 255, 255)
EM_GREEN  = (168, 255, 96)              # #A8FF60  강조 연두
EM_YELLOW = (251, 255, 86)              # #FBFF56  강조 노랑 (NEON 과 같은 값)
DLG_C     = (126, 200, 255)             # #7EC8FF  대사 자막 하늘색 (2026-08-31)
CHANNEL_C = (200, 200, 200)

# ── 타이포 ────────────────────────────────────────────────────
FONT_DIR   = "/Users/kimyiseul/Library/Fonts"
F_TITLE    = f"{FONT_DIR}/JalnanGothic.otf"           # 상단 타이틀 — 잘난체 고딕 (2026-08-31)
F_BAR      = f"{FONT_DIR}/Pretendard-SemiBold.ttf"    # 흰 바 제목
F_SUB      = f"{FONT_DIR}/JalnanGothic.otf"           # 나레이션 자막 — 잘난체
F_CH       = f"{FONT_DIR}/Pretendard-Medium.ttf"      # 채널명
F_EM       = f"{FONT_DIR}/BMEULJIROTTF.ttf"           # 강조 자막 — 배달의민족 을지로체
F_DLG      = f"{FONT_DIR}/BMEULJIROTTF.ttf"           # 대사 자막 — 을지로체 + 하늘색으로 구분

SZ_TITLE   = 120                        # 상한 — 폭에 맞춰 자동 확대/축소
SZ_BAR     = 64                          # 상한 — 흰 바 폭을 거의 꽉 채움
BAR_H      = 0.055                       # 흰 바 높이(세로 비율) — style.bar 를 줄 때만 그려짐
SZ_SUB     = 66
SUB_TRACK  = -2                          # 자막 자간 (px)
SUB_CHUNK  = 10                          # 자막 한 덩어리 목표 글자수
SPEED      = 1.3                         # 최종 배속 — 고정값 (2026-08-24 사용자 확정, 변경 금지)
SZ_CH      = 34
EM_SIZE_R  = 1.14                        # 강조 폰트 크기 비율 (기준 자막 대비)

# ── 캐릭터 (자나스 레퍼런스 실측: 폭 12.7%, 정중앙, 이미지 상단에 걸침) ──
CHAR_H       = 0.062                    # 화면 높이 대비 캐릭터 높이
CHAR_OVERLAP = 0.018                    # 이미지 상단을 이만큼 덮는다
CHAR_SEC     = 0.0                      # 캐릭터 끔 (2026-08-30 사용자 지시). 되살리려면 2.0

# ── 워터마크 (자나스처럼 이미지 하단에 작게 반투명 채널명) ──────
# 항상 켜둔다. 캡처가 퍼질 때 채널명이 같이 나가는 역할.
# 이미지 안쪽(CONTENT_BOT 0.710 위)에 두어 자막(0.745)과 겹치지 않게 한다.
WATERMARK    = "@썰푸는휴지"
WM_Y         = 0.686
WM_SIZE      = 26
WM_ALPHA     = 145

# ── 애니메이션 ────────────────────────────────────────────────
FPS          = 30
KEN_ZOOM     = 0.09                     # 구간 동안 천천히 확대/축소하는 폭
INTRO_SEC    = 0.45                     # 첫 이미지 페이드인 시간
SLIDE_SEC    = 0.32                     # 이미지 교체 슬라이드 시간

SIDE_PAD       = 56                     # 자막 좌우 안전 여백
TITLE_PAD      = 80                     # 모바일 플레이어가 좌우를 살짝 잘라먹어 24→80 (2026-08-31)
TITLE_TRACK    = -1                     # 타이틀 자간 (px)
BAR_PAD        = 40
TITLE_STROKE   = 10                     # 타이틀 검정 외곽선 두께
SUB_STROKE     = 6                      # 자막 외곽선 (밝은 이미지 위에서도 읽히게)
SUB_MAX_LINES  = 2


def px(ratio: float) -> int:
    """세로 비율 → 픽셀"""
    return int(round(ratio * H))
